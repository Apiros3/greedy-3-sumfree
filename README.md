# Greedy Three-Sumfree Sequences

This repository contains the standalone Lean verification layer for the proved families used in:
LINK To be added.

The formal entry point is `GreedyThreeSumfree.PaperProofs`.

Most of the proofs were generated with the aid of codex, then verified by myself.

## Verify

```sh
cd formal
lake build
```

This builds the Lean package using the toolchain in
`formal/lean-toolchain`.

